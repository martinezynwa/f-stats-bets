import type { ViewStyle } from 'react-native'

import { Colors } from './colors'

export const commonLogoBackgroundStyles: ViewStyle = {
  backgroundColor: Colors.iconBackground,
  padding: 4,
  borderRadius: 8,
}
